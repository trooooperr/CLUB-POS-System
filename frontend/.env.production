# Production environment
# When deployed, frontend is served from the same domain as the backend.
# Leave VITE_API_URL empty so api.js falls back to window.location.origin.
VITE_API_URL=

# Proxy target not needed in production
VITE_PROXY_TARGET=
